FROM node:lts-alpine

# Install required system packages
RUN apk add --no-cache curl git bash zsh openssh-client

WORKDIR /app

# Copy package files
COPY package*.json ./

# Install dependencies
RUN npm install

# Expose Vite port
EXPOSE 5173

# Keep container running
CMD ["npm", "run", "dev"]